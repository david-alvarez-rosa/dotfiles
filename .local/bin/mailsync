#!/usr/bin/env sh

sync_one() { flock "${XDG_RUNTIME_DIR:-/tmp}/mbsync-$1.lock" mbsync -c ~/.config/isync/mbsyncrc "$1"; }

case "${1:-}" in
	"") sync_one personal ;;
	*)  sync_one "$1" ;;
esac

[ -n "${MAILSYNC_NO_INDEX:-}" ] || emacsclient -e '(dalvrosa/mu4e-index-soon)' >/dev/null 2>&1 || true
