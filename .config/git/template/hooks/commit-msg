#!/usr/bin/env sh

sed '/^# -* >8 -*/,$d' "$1" | grep -qiE '^co-authored-by:.*(claude|anthropic)|generated with.*claude|^claude-session:|claude\.ai/code/session' || exit 0
echo "commit-msg: Attribution blocked" >&2

exit 1
